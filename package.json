{
  "name": "great-reading",
  "version": "1.0.0",
  "description": "GreatReading - English language learning reading application",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && uv run uvicorn app.main:app --reload --port 3000",
    "dev:frontend": "cd frontend && npm run dev",
    "test": "concurrently \"npm run test:backend\" \"npm run test:frontend\"",
    "test:backend": "cd backend && uv run pytest",
    "test:frontend": "cd frontend && npm test -- --run",
    "install:all": "cd backend && uv sync && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
